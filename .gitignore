config.php
/pse_devserver.php
/uploads/*
!/uploads/.gitkeep
!/uploads/.htaccess
# Catálogo importado (esquinablanca + tugou): se versiona para que el seed 041
# reproduzca las imágenes en producción. El resto de uploads/ queda ignorado.
!/uploads/library/
/uploads/library/*
!/uploads/library/importados/
# Assets de marca del landing (hero, ecología, logo genérico de clientes):
# se versionan para que el seed/migración los reproduzca en producción.
!/uploads/library/greenbags/
/install/installed.lock
/install/.htaccess
/respaldo/
.DS_Store
.claude/
*.log
/uploads/logs/
